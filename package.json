{
  "name": "miniprogram-1",
  "version": "1.0.0",
  "description": "中央音乐学院音乐人才对接小程序 - 云函数测试",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"],
    "moduleNameMapper": {
      "wx-server-sdk": "<rootDir>/tests/helpers/cloudMock.js"
    }
  }
}
